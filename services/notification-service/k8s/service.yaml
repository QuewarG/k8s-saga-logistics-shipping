apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: saga-shipping
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
  - name: http
    protocol: TCP
    port: 5008
    targetPort: 5008